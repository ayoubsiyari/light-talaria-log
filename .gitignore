node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example
# Step 13 local disk chunk store (CDN stub) — seeded at dev-server start
/data/
# Standalone Vite landing package (local only; chart app uses src/marketing-content)
/landing/
# Local Talaria-log reference dump (large; not part of the app)
/Talaria-log/
services/api/node_modules
services/api/dist
